# Copy to .env.production and replace with real values.
# Never commit real secrets.

# Docker image to deploy (GitHub Container Registry by default)
IMAGE=ghcr.io/conradstorz/retire_your_way:latest

# Managed PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/database
